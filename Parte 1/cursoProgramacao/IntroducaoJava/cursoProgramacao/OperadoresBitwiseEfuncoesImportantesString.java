package cursoProgramacao;

public class OperadoresBitwiseEfuncoesImportantesString {

	public static void main(String[] args) {
		
		//operadores bitwise -- operacao bit a bit
		
		//&- operacao E |- operacao OU ^- operacao OU EXCLUSIVO 
		
		//STRING
		
		// Formatar: toLowerCase(), toUpperCase(), trim()
		
		// toLowerCase(); -- deixa tudo minusculo
		// toUpperCase(); -- tudo maiusculo
		// trim(); -- elimina espacos em branco no final
		
		// Recortar: substring(inicio), substring(inicio, fim)
		
		//substring(inicio) -- recortar apartir do inicio e vai ate o fim
		//substring(inicio, fim) -- recorta o inicio e vai ate o fim definido
		
		// Substituir: Replace(char, char), Replace(string, string)
		
		//Replace(char, char) -- troca um caractere por outro
		//Replace(string, string) -- troca uma string por outra
		
		
		// Buscar: IndexOf, LastIndexOf
		
		//IndexOf -- mostra a primeira ocorrencia de algo
		//LastIndexOf -- ultima ocorrencia de algo 
		
		// str.Split(" ")
		
		// recorta uma string, divididas em espaco em branco ou etc, voce informa nas aspas duplas onde e pra ele cortar
	}

}
