package CursoObservacoes;

import java.util.Arrays;
import java.util.Set;

public class InterfaceSet {

	public static void main(String[] args) {
/*
		Set<String> set = new HashSet<>();//hashset a mais rapida mas nao garante a ordem dos elementos
		//Set<String> set = new TreeSet<>(); ordena por ordem alfabetica
		//Set<String> set = new LinkedHashSet<>(); mantem a ordem dos elementos inseridos
		
		set.add("TV");
		set.add("Notebook");
		set.add("Tablet");
		
		// set.removeIf(x -> x.length() >= 3); remove tudo maior ou igual a 3 letras
		// set.removeIf(x -> x.charAt(0) == 'T'); remove tudo que tem a letra T no inicio, e etc...
				
		System.out.println(set.contains("Notebook")); //contains, se existe o elemento notebook, retorna true
	
		for (String p : set) {
			System.out.println(p);
		}
		
		Set<Integer> a = new TreeSet<>(Arrays.asList(0,2,4,5,6,8,10));
		Set<Integer> b = new TreeSet<>(Arrays.asList(5,6,7,8,9,10));
		
		//union	
		Set<Integer> c = new TreeSet<>(a);
		c.addAll(b); //faz a uniao do b com um conjunto c
		System.out.println(c);
		
		//intersection
		Set<Integer> d = new TreeSet<>(a);
		d.retainAll(b); // adiciona elementos comuns entre o a e o b, e adiciona na lista d
		System.out.println(d);
		
		//difference
		Set<Integer> e = new TreeSet<>(a);
		e.removeAll(b); // remove todo mundo que ta no conjunto b
		System.out.println(e);
		
		*/
			
	}
}
